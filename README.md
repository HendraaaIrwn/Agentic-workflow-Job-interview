## PDF Export Note

This project uses WeasyPrint to export the final report into PDF.
On macOS, WeasyPrint may require additional native libraries installed via Homebrew.
If PDF export is unavailable, the system will save the report as Markdown in the `output/` folder.
# Agentic-workflow-Job-interview
